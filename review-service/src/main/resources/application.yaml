spring:
  application:
    name: review-service

  data:
    mongodb:
      uri: mongodb://user:password@localhost:27017/reviewdb?authSource=admin
      username: user
      password: password

server:
  port: 8081

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
